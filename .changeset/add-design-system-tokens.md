---
"varena": minor
---

Add built-in design system tokens

- Add `varena/design-system` entry point with animation, border, color, effect, filter, interactivity, layout, palette, spacing, and typography tokens
